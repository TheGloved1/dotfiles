# Environment variables (example/template)
# Copy this to ~/.env and fill in your values:
#   cp ~/.env.example ~/.env
# Secrets are NOT tracked by yadm. Add ~/.env to .yadmignore.

# Example vars — remove and add your own:
# API_KEY=your-key-here
# DATABASE_URL=postgres://...
# OPENAI_API_KEY=sk-...